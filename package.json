{
  "name": "superset-agent-fleet",
  "private": true,
  "type": "module",
  "description": "A pixel-art city block that shows a Superset agent fleet at work: each orchestrator as the chef of its own restaurant, the sessions it drives at the tables, and everyone else out on the street at the food carts.",
  "scripts": {
    "start": "bun server.js",
    "dev": "bun --watch server.js",
    "format": "prettier --write .",
    "check": "prettier --check ."
  }
}
